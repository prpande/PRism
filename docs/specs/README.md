# Specs index

Per-slice / per-task design docs. New specs land at `docs/specs/YYYY-MM-DD-<topic>-design.md` (output of the brainstorming skill). Each entry below names its matching plan under `docs/plans/`, the PR(s) that landed it, and its deferred work. Deferred / rejected alternatives are recorded per the [`.ai/docs/documentation-maintenance.md`](../../.ai/docs/documentation-maintenance.md) deferred-work convention, all surfaced in a spec's `## Deferred work` section: `[Defer]` items get a `deferred`-labelled GitHub issue (the system of record) linked from that section, while `[Skip]`/`[Superseded]` items are recorded inline only (no issue). Older specs instead carry a frozen `<source>-deferrals.md` sidecar (historical record — not migrated); reference it where present.

When a spec's status changes, move its entry to the right group and add the PR reference. Per [`.ai/docs/documentation-maintenance.md`](../../.ai/docs/documentation-maintenance.md) (and [`CLAUDE.md`](../../CLAUDE.md) for Claude-only auto-review workflow), this update lands in the same PR that ships the implementation.

## Implemented

- [`2026-05-05-foundations-and-setup-design.md`](2026-05-05-foundations-and-setup-design.md) — S0+S1 walking skeleton; plan: [`../plans/2026-05-05-foundations-and-setup.md`](../plans/2026-05-05-foundations-and-setup.md). Shipped.
- [`2026-05-06-pat-scopes-and-validation-design.md`](2026-05-06-pat-scopes-and-validation-design.md) — PAT scope set + validation flow; plan: [`../plans/2026-05-06-pat-scopes-and-validation.md`](../plans/2026-05-06-pat-scopes-and-validation.md). Shipped.
- [`2026-05-06-prism-validation-prompt-set-design.md`](2026-05-06-prism-validation-prompt-set-design.md) — Validation prompt corpus. Shipped.
- [`2026-05-06-run-script-reset-design.md`](2026-05-06-run-script-reset-design.md) — `run.ps1` reset/orchestration; plan: [`../plans/2026-05-06-run-script-reset.md`](../plans/2026-05-06-run-script-reset.md). Shipped.
- [`2026-05-06-inbox-read-design.md`](2026-05-06-inbox-read-design.md) — S2 inbox (read); plan: [`../plans/2026-05-06-s2-inbox-read.md`](../plans/2026-05-06-s2-inbox-read.md). PR #4. Shipped.
- [`2026-05-07-appstatestore-windows-rename-retry-design.md`](2026-05-07-appstatestore-windows-rename-retry-design.md) — Windows AV/indexer rename race fix; plan: [`../plans/2026-05-07-appstatestore-windows-rename-retry.md`](../plans/2026-05-07-appstatestore-windows-rename-retry.md). PR #16. Shipped.
- [`2026-05-07-flaky-spa-fallback-test-fix-design.md`](2026-05-07-flaky-spa-fallback-test-fix-design.md) — Deterministic wwwroot stub for SPA fallback test; plan: [`../plans/2026-05-07-flaky-spa-fallback-test-fix.md`](../plans/2026-05-07-flaky-spa-fallback-test-fix.md). PR #16. Shipped.
- [`2026-05-07-docs-sync-and-auto-update-design.md`](2026-05-07-docs-sync-and-auto-update-design.md) — Docs sync + restructure + auto-review policy; plan: [`../plans/2026-05-07-docs-sync-and-auto-update.md`](../plans/2026-05-07-docs-sync-and-auto-update.md). PR #17. Shipped.
- [`2026-05-10-multi-account-scaffold-design.md`](2026-05-10-multi-account-scaffold-design.md) — S6 PR0 multi-account storage-shape scaffold (V4→V5 state migration `MigrateV4ToV5` + `github.accounts: [...]` config rewrite + token-cache versioning, no interface/wire changes — v2 multi-account brainstorm absorbs those); plan: [`../plans/2026-05-10-multi-account-scaffold.md`](../plans/2026-05-10-multi-account-scaffold.md); deferrals: [`2026-05-10-multi-account-scaffold-deferrals.md`](2026-05-10-multi-account-scaffold-deferrals.md). PR #53. Shipped.
- [`2026-05-15-s6-polish-and-distribution-design.md`](2026-05-15-s6-polish-and-distribution-design.md) — S6 polish + distribution: Settings page (Appearance + Inbox sections + Connection) with optimistic save + toast rollback, Replace token UX driving the `/api/auth/replace` identity-change rule (drafts preserved, Node IDs cleared, IdentityChanged SSE projection), keyboard cheatsheet overlay, branded LoadingScreen + icon-asset shrink (sharp-derived 256×256 PNG + 32×32 favicon), full a11y audit pass (axe-core sweep + WCAG AA contrast + SR-only badge labels), `publish.yml` manual-dispatch workflow producing `PRism-win-x64.exe` + `PRism-osx-arm64` to a draft Release with SHA-pinned actions + Dependabot, first-run trust copy for SmartScreen + Gatekeeper, viewport screenshot regression (this PR's `no-layout-shift-on-banner.spec.ts` with `getBoundingClientRect` invariance + per-platform-pinned screenshot baseline), README graduation, spec-doc updates (§ 11 Settings, § Distribution, roadmap S6 row, this specs/README entry). Deferrals sidecar: [`2026-05-15-s6-polish-and-distribution-deferrals.md`](2026-05-15-s6-polish-and-distribution-deferrals.md). Plan: [`../plans/2026-05-15-s6-polish-and-distribution.md`](../plans/2026-05-15-s6-polish-and-distribution.md) (9-PR cut after PR0). PR0 (#53 — multi-account storage-shape scaffold), PR1 (#69 — backend prefs allowlist + nested GET /api/preferences + /api/submit/in-flight + SubmitLockRegistry held-set), PR2 (#70 — backend POST /api/auth/replace + identity-change rule + IdentityChanged SSE + qualified structured logs), PR3 (#71 — frontend Settings page + Header three-tab nav + Toast 'success' kind + usePreferences key-scoped rollback + Playwright e2e), PR4 (#72 — frontend Replace token UX + /test/submit/{hold,release-hold} backend hooks), PR5 (#73 — cheatsheet overlay with ARIA APG focus + capture-phase Esc + modal-aware focus skip), PR6 (#74 — branded LoadingScreen + Header logo + favicon + icon-asset shrink via sharp), PR7 (#75 — a11y audit DoD-checkpoint with --text-3 contrast bump + sr-only badge companions + tab-strip ", N items" SR text), PR8 (#76 — publish.yml workflow + first-run trust copy + dependabot + CI unresolved-pin guard), PR9 (this PR — `no-layout-shift-on-banner.spec.ts` viewport regression + new `/test/emit-pr-updated` deterministic banner trigger + README graduation + spec-doc updates). Shipped.
- [`2026-05-11-s5-submit-pipeline-design.md`](2026-05-11-s5-submit-pipeline-design.md) — S5 submit pipeline: stepwise `IReviewSubmitter` capability seam, resumable `SubmitPipeline` state machine with multi-marker-match defense, GraphQL pending-review pipeline, per-PR submit lock, foreign-pending-review prompt with TOCTOU defense + IsResolved badge, explicit-button stale-`commitOID` retry, closed/merged bulk-discard with `deletePullRequestReview` courtesy, AI validator placeholder card + Ask AI button + static empty state (the originally-planned interactive drawer was cut after doc-review), V4 schema migration for `DraftComment.ThreadId`. Deferrals sidecar: [`2026-05-11-s5-submit-pipeline-deferrals.md`](2026-05-11-s5-submit-pipeline-deferrals.md) (`status: open` — remaining entries are forward-looking S6/P0+ items + PR0b; records brainstorm-time + doc-review-time deferrals, FYI observations, and residual risks). Plan: [`../plans/2026-05-11-s5-submit-pipeline.md`](../plans/2026-05-11-s5-submit-pipeline.md) (8-PR cut — PR6 folded into PR4, PR0 pre-split into PR0a capability-split-and-gates + PR0b state-leak-fix; carries a "Doc-review revisions (2026-05-12)" section with the ce-doc-review-derived deltas). PR0a (#44 — capability split + C6/C7/C9 gates, resolved in [`../spec/00-verification-notes.md`](../spec/00-verification-notes.md)), PR1 (#45 — 7-method `IReviewSubmitter` GraphQL adapter), PR2 (#46 — `SubmitPipeline` state machine + v3→v4 migration), PR3 (#47 — backend submit endpoints `POST /submit` + `/submit/foreign-pending-review/{resume,discard}` + `/drafts/discard-all`, the five submit-* SSE events, `SubmitLockRegistry` per-PR lock, composer marker-prefix collision rejection, verdict-clear `JsonElement` patch wire-shape, `SensitiveFieldScrubber` extension), PR4 (#48 — frontend Submit confirmation dialog + `useSubmit` hook with multi-tab guard + Submit Review button enable rules + enabled verdict picker — frozen-during-pipeline, click-to-clear — + `PreSubmitValidatorCard` aiPreview canned data + Ask AI button + static "coming in v2" empty state + in-flight-submit recovery badge), and PR5 (#49 — foreign-pending-review modal + Resume/Discard confirmation sub-modal + Snapshot A→B / IsResolved imported-drafts banner + stale-`commitOID` retry banner with not-yet-Reloaded variant + closed/merged bulk-discard button + confirmation modal + `useSubmitToasts` for the `submit-duplicate-marker-detected` / `submit-orphan-cleanup-failed` toasts + TOCTOU-409 toast) merged. PR7 (#50) lands the working `FakeReviewSubmitter` + `/test/submit/*` test hooks + eight Playwright specs (demo happy path, retry-from-each-step DoD (b), foreign-pending-review Resume/Discard/Cancel DoD (c)+(d), stale-`commitOID` recreate DoD (e), lost-response marker adoption, closed/merged bulk-discard + orphan-cleanup toast, multi-tab per-PR-lock, composer marker-prefix collision). PR0b (Playwright multi-spec state-leak fix — *this PR*) bakes `workers: 1` into `playwright.config.ts` (closes the `--workers=1` interim flag PR7 shipped with), un-fixme's the three carved-out S4 specs (`s4-keep-anyway-survives-reload`, `s4-multi-tab-consistency`, `s4-reconciliation-fires`), and resolves three real bugs uncovered along the way (`/test/advance-head` re-seeds `IActivePrCache` symmetric to PR7's `/test/reset` hardening; multi-tab spec rewritten from `browser.newContext() × 2` to one context + two pages; override-chip regex `/override/i` replaced with `.locator('.chip.chip-override')`). Shipped.
- [`2026-06-02-merged-pr-history-design.md`](2026-06-02-merged-pr-history-design.md) — P4-D2 merged/closed PR history (pre-gate pull-forward): bounded "recently wrapped up" inbox section + read-only detail gap closure. Plan: [`../plans/2026-06-02-merged-pr-history.md`](../plans/2026-06-02-merged-pr-history.md). Deferrals sidecar: [`2026-06-02-merged-pr-history-deferrals.md`](2026-06-02-merged-pr-history-deferrals.md) (8 `[Defer]` + 5 `[Decision]` entries). PR1 (#103 — backend `recently-closed` inbox section: two-sub-query participant-union, `MergedAt`/`ClosedAt` fields threaded through `RawPrInboxItem`/`PrInboxItem`, REST-enricher close-state population, empty-`HeadSha` filter exemption for closed rows, `InboxHistoryConstants` constants, `inbox.sections.recentlyClosed` config key + `/api/preferences` key + config patch). PR2 (#105 — frontend section: collapsed-by-default `InboxSection` with `defaultOpen` prop, text-primary Merged/Closed badge, suppressed urgency cues on frozen rows, truncation footer, Settings toggle). PR3 (this PR — read-only detail gaps: `Pr` contract + `types.ts` surfaces `mergedAt`/`closedAt`; `PrHeader` merged/closed status label via extracted `formatAge` → `relativeTime.ts`; read-only Drafts tab threaded with `prState || contextReadOnly`; live merge/close transition banner via full cross-tier extension — `ActivePrPoller` open→done state-change emit + `ActivePrUpdatedWire` `isMerged`/`isClosed` fields + `BannerTransition` superseding `BannerRefresh`; primary + cross-iteration diff-unavailable 422 mapping fixing a lying doc-comment + dead `RangeUnreachableException` handler; `FakeReviewBackingStore.IsClosed` fix + `FakePrReader.GetPrDetailAsync` close-timestamp derivation test-hook corrections; read-only audit e2e `recently-closed-readonly.spec.ts`). Shipped.

## In progress

- [`2026-06-06-first-run-welcome-landing-design.md`](2026-06-06-first-run-welcome-landing-design.md) — Phase 4 (#212) warmer first-run landing. A new `/welcome` route + `WelcomePage` (brand icon + wordmark + value tagline + three benefit rows + "Get started" CTA + Help/Send-feedback footer stubs) shown ONLY on a true first run (`!authState.hasToken`); `App.tsx` routes all four unauthed guard sites (`/`, `/settings`, `/pr/*`, `*`) to `/welcome` when `!hasToken`, else `/setup`, so a token-rejected re-auth session and Settings→Replace never see the welcome hero. Plus a first-run-only "← Back" on `/setup` and an inline GitHub mark (shared `GitHubMark`) on its heading. Frontend-only, no backend/auth change. B1-gated (UI-visual). Copy is placeholder — human rewrite is #222; PAT fine-grained-vs-classic accuracy is #213; footer Help/Feedback links are stubs wired by #210/#211. Plan: [`../plans/2026-06-06-first-run-welcome-landing.md`](../plans/2026-06-06-first-run-welcome-landing.md). Implementation PR #230 open. In progress.

- [`2026-06-05-condense-pr-header-on-scroll-design.md`](2026-06-05-condense-pr-header-on-scroll-design.md) — Phase 3 (#128) collapsible PR-detail header + Files-toolbar density trim. A manual double-chevron toggle (sibling of the sub-tab `role="tablist"`, never a child — avoids `aria-required-children`) reflows the header meta to a single compact row (keeps the title + CI/mergeability chips, hides repo·#/status/author/branch/iterationLabel) backed by a per-PR, session-only in-memory store (`usePrHeaderCollapsed`, mirrors `useTabScrollMemory`); plus an always-on CSS trim of the over-padded Files toolbar (~77px→~51px). Criteria 1–3 of the issue (sticky tree, confined scroll, both shells) already shipped via the viewport-bound Files layout; criterion 4 was reframed (collapse-on-scroll → manual condense) — disposition to be posted on #128 at close. Plan: [`../plans/2026-06-05-condense-pr-header.md`](../plans/2026-06-05-condense-pr-header.md). Implementation PR open. In progress.

- [`2026-06-03-issue-resolution-workflow-design.md`](2026-06-03-issue-resolution-workflow-design.md) — agent-driven, proof-carrying, risk-gated issue workflow shipped as a **guidance runbook** ([`../../.ai/docs/issue-resolution-workflow.md`](../../.ai/docs/issue-resolution-workflow.md)): two-axis tier×risk classification, per-tier pipelines, proof-of-fix contract, gate substitution (hands-off T3 in scope), `@claude`-advisory-only scoping, human-merge as the safety boundary. No CI enforcement (deliberate). Plan: [`../plans/2026-06-03-issue-resolution-workflow.md`](../plans/2026-06-03-issue-resolution-workflow.md). Deferrals: [`2026-06-03-issue-resolution-workflow-design-deferrals.md`](2026-06-03-issue-resolution-workflow-design-deferrals.md) (mechanical enforcement `[Skip]` + restrict-T3 `[Skip]` + auth-proof-item `[Skip]`). Docs complete in one PR with no follow-up work; promote to Implemented on merge.

- [`2026-05-06-s3-pr-detail-read-design.md`](2026-05-06-s3-pr-detail-read-design.md) — S3 PR detail (read); plan: [`../plans/2026-05-06-s3-pr-detail-read.md`](../plans/2026-05-06-s3-pr-detail-read.md); deferrals: spec [`2026-05-06-s3-pr-detail-read-deferrals.md`](2026-05-06-s3-pr-detail-read-deferrals.md) (extended through PR8 with frontend preflight `[Defer]` entries — sub-1180px responsive sheet, AI focus dot column, inline toast for viewed-checkbox rollback, file-viewed graph-walk semantics, Shiki syntax highlighting, `react-diff-view` integration, diff-mode persistence, d-key tooltip, plus the PR6 EventSource and PrHeader skeleton entries; PR5's `[Skip]` multimap-vs-(a)/(b) and `[Superseded]` cookie-only-vs-cookie-OR-header still recorded; PR #32 `[Skip]` Option B client-side jsdiff vs Option A server-side `patch` parse), plan [`../plans/2026-05-06-s3-pr-detail-read-deferrals.md`](../plans/2026-05-06-s3-pr-detail-read-deferrals.md) (plan rigor pass + post-merge implementation deferrals from PR4 / PR5 / PR6 / PR7 / PR8 — including PR5's `[Defer]` SensitiveFieldScrubber pipeline wire-up + `[Superseded]` body-cap implementation shift). All five backend PRs shipped: PR1 state migration (PR #14), PR2 iteration clustering (PR #15), PR3 `IReviewService` extensions (PR #19), PR4 `PrDetailLoader` + backend endpoints (PR #21), PR5 SSE per-PR fanout + active-PR poller + `SessionTokenMiddleware` + cookie stamping + Origin tightening + `SensitiveFieldScrubber` (PR #22). Frontend PRs 6–9 shipped: PR6 PR-detail shell — page, routing, sub-tabs, banner, EventSource silence-watcher (PR #25), PR7 Files tab — file tree, iteration strip, commit picker, diff pane stub (PR #27), PR8 Diff Pane + Markdown pipeline — custom side-by-side diff renderer, jsdiff word overlay, Mermaid + GFM with strict-mode sanitization, existing-comment widgets (PR #28; `react-diff-view` integration remains deferred — see deferrals sidecar), PR9 Overview tab — `OverviewTab` assembly + `AiSummaryCard` / `PrDescription` / `StatsTiles` / `PrRootConversation` / `ReviewFilesCta` components, `useAiSummary` hook, `aiSummary` API helper, and the new `GET /api/pr/{ref}/ai/summary` backend endpoint resolving `IPrSummarizer` via `IAiSeamSelector` (PR #30). Post-PR8 fix: PR #32 — server-side parsing of GitHub's per-file `patch` field via new `PRism.Core.Contracts.PatchParser`, closing the PR4/PR8 contract gap that left `FileChange.Hunks` always empty. Task 10 (documentation updates) and Task 11 (contract tests against the frozen `api-codex` PR) remain.
- [`2026-05-06-architectural-readiness-design.md`](2026-05-06-architectural-readiness-design.md) — Cross-cutting structural items gated to slices. Mixed status: *Now*-gate items (banned-API analyzer, DI extension methods) shipped per `docs/roadmap.md` § Architectural readiness; named-records item still TBD; S3 / S4 / S5 / P0+ items remain open.
- [`2026-05-08-multi-agent-ai-rules-design.md`](2026-05-08-multi-agent-ai-rules-design.md) — Shared `.ai/docs/` SSOT + slim `CLAUDE.md` + Cursor rules; plan: [`../plans/2026-05-08-multi-agent-ai-rules.md`](../plans/2026-05-08-multi-agent-ai-rules.md). In progress.
- [`2026-05-18-frozen-pr-contract-tests-design.md`](2026-05-18-frozen-pr-contract-tests-design.md) — Frozen-PR contract tests (live-GitHub integration suite); plan: [`../plans/2026-05-18-frozen-pr-contract-tests.md`](../plans/2026-05-18-frozen-pr-contract-tests.md). Supersedes deferred S3 Task 11 (originally targeting `mindbody/Api.Codex`). Redirects the suite to PRism's own merged-PR history (PRs #1, #16, #19, #22, #28) + adds an 8-PR cross-author validation set from `prpande/ShaktimaanAI`. Implementation also performed the first-ever calibration of `IterationClusteringCoefficients` (new `MinimumBoundaryGapSeconds=900`, `HardFloorSeconds 300→60`, `DegenerateFloorFraction 0.5→0.6`, `MadK 3→4`, MAD=0 fallback uses second-largest distance, single-commit PRs return `Ok+1` not `Low+null`). Runbook: [`../contract-tests.md`](../contract-tests.md). In progress.
- [`2026-05-18-on-disk-log-writer-design.md`](2026-05-18-on-disk-log-writer-design.md) — On-disk log writer for `PRism.Web` (closes the [Defer] entry at `2026-05-11-s5-submit-pipeline-deferrals.md:848`). Adds a `FileLoggerProvider` registered alongside the framework's Console + Debug providers, writes UTF-8 plain-text lines to `<dataDir>/logs/prism-YYYY-MM-DD.log`, rolls over at local midnight, sweeps files older than 14 days on startup, and redacts `SensitiveFieldScrubber.BlockedFieldNames` (extended with `login`) inside the sink before lines hit disk. Console + Debug providers stay unchanged — the universal factory-level decorator (S3 PR5 deferral) is explicitly out of scope, captured in [`2026-05-18-on-disk-log-writer-deferrals.md`](2026-05-18-on-disk-log-writer-deferrals.md). Plan: [`../plans/2026-05-18-on-disk-log-writer.md`](../plans/2026-05-18-on-disk-log-writer.md). Implementation PR open with `FileLoggerProvider` + `FileLogger` + `LogTemplateFormatter` + `FileLogEvent` + `SensitiveFieldScrubber` split (added `ScrubFieldName` + `login` blocklist) + `AddPRismFileLogger` extension wired pre-Build in `Program.cs`; 4 test files / 25 logging tests (12 lifecycle + 11 invariants + 2 integration end-to-end via `WebApplicationFactory<Program>`); Path A extension routes `OpenAppendStream` failures to `_writeFailureCount` (closes a writer-task discipline gap the plan reviewers missed). In progress.
- [`2026-05-28-v1-completion-roadmap-design.md`](2026-05-28-v1-completion-roadmap-design.md) — v1 ship roadmap decomposing the post-S6 work into three sequential phases: Phase 1 single-instance enforcement (separate brainstorm-then-spec; closes the largest credible v1 data-loss path), Phase 2 README restructure + `CONTRIBUTING.md` extraction + doc/index sweep (bat/ripgrep style; one PR), Phase 3 `v0.1.0` tag (Windows-only) + `publish.yml workflow_dispatch` + post-publish reconciliation (macOS Apple Silicon defers to v0.1.1). Plan: [`../plans/2026-05-28-v1-completion-roadmap.md`](../plans/2026-05-28-v1-completion-roadmap.md). Deferrals sidecar: [`2026-05-28-v1-completion-roadmap-deferrals.md`](2026-05-28-v1-completion-roadmap-deferrals.md) (6 `[Skip]` entries from spec ce-doc-review — all false positives, pedantic, or folded into spec text; no `[Defer]` items). In progress.
- [`2026-06-02-electron-desktop-shell-design.md`](2026-06-02-electron-desktop-shell-design.md) — v0.2.0 Electron desktop shell: Electron main + .NET sidecar (spawn, stdout-port handshake, `/api/health` gate, sandboxed `BrowserWindow` on `127.0.0.1`), single-instance via `requestSingleInstanceLock` (closes the deferred Phase 1 single-instance data-loss path), recycle-resistant parent-liveness watchdog, Host-header DNS-rebinding defense, unsigned cross-platform installers + `TESTING.md`, `v0.2.*` matrix publish workflow. Deferrals: [`2026-06-02-electron-desktop-shell-deferrals.md`](2026-06-02-electron-desktop-shell-deferrals.md). Plan: [`../plans/2026-06-02-electron-desktop-shell.md`](../plans/2026-06-02-electron-desktop-shell.md). In progress.
- [`2026-06-04-pr-tab-state-keepalive-design.md`](2026-06-04-pr-tab-state-keepalive-design.md) — Preserve PR-detail tab view-state across navigation via a keep-alive architecture: a persistent `PrTabHost` keeps every open PR view mounted (`hidden` when inactive) instead of React Router unmounting them, so scroll / sub-tab / file-selection / composer state survive a trip to the Inbox or another tab. Sub-tab moves from URL into component state (deep-linking dropped); a shared `data-app-scroll` scroller with per-tab `scrollTop` save/restore plus a `data-files-active` marker (revised during planning from per-view scroll containers to avoid regressing the hardened diff-scroll model — the primary risk); refetch-on-focus for freshness. Deferred work tracked in [#161](https://github.com/prpande/PRism/issues/161) (LRU cap / hidden-tab SSE pausing / Inbox keep-alive) and [#160](https://github.com/prpande/PRism/issues/160) (read receipts) per the spec's `## 9. Deferred work`. Plan: [`../plans/2026-06-04-pr-tab-state-keepalive.md`](../plans/2026-06-04-pr-tab-state-keepalive.md). PR1 (#166 — `PrTabHost` + routing swap + sub-tab-in-state + child prop migration + `:has(.files-tab)`→`data-files-active` marker scroll-model fix + per-tab `scrollTop` save/restore + diff-scroll regression gate) and PR2 (#168 — data freshness: `useActivationTransition`-driven refetch + `clearUnread` + latched-banner clear on tab re-activation, OQ1 option (a)/OQ8, with OQ5 reset-to-first-file and OQ6 non-destructive-refetch guards) merged; PR3 (this PR — test hardening: restored title-on-resolve coverage in the state-based architecture, hidden-view keyboard/a11y isolation guard, and a Playwright keep-alive e2e covering sub-tab/selected-file/scroll preservation across PR→Inbox→PR in-app navigation plus the backgrounded `pr-updated` banner latch-and-clear; two-PR-tab independence stays unit-covered rather than e2e because the fake serves only `acme/api/123`) open. In progress.

- [`2026-06-07-font-size-control-design.md`](2026-06-07-font-size-control-design.md) — Phase 6 (#135) font-size control for PR-detail content. A Settings "Content size" slider (native range + five growing-"a" glyphs, no labels) scales the font-size of all *data* across the PR-detail tabs (Overview/Files/Drafts) while interactive/navigational chrome stays fixed (owner "Option 2"; comment author/time metadata stays fixed too — I-A). Mechanism: new `ui.contentScale` string enum (`xs|s|m|l|xl`, default `m`) persisted exactly like `density` (UiConfig → ConfigStore allowlist/patch → UiPreferencesDto → GET/PATCH `/api/preferences` → `UiPreferences.contentScale` → `PreferencesContext` set/read/write); `applyContentScaleToDocument` sets `data-content-scale` on `<html>`, `AppearanceSync` applies it on load, and `tokens.css` maps the attribute to a global `--content-scale` multiplier (default `1`, so the middle step is a true no-op and baselines are unchanged) consumed only by enumerated content hooks (`.markdown-body` universal prose + `.diffTable`, `.aiSummaryCard`/`.aiSummaryCategory`, `.statsTileValue`/`.statsTileLabel`, `.aiHunk`, `.markdownRaw`, `.prDescriptionTitle`/`.prDescriptionBody`); chrome keeps the bare `--text-*` tokens. C1 (PrDescription same-element cascade) fixed by scaling the winning class directly; I1 (pinned inline `code` at 3 sites) made relative `0.92em`. B1-gated (UI-visual). Rejected/skip decisions recorded inline in the spec's Decision log / Out-of-scope (Option 1 region-token override, scaling comment metadata, scaling chrome) — `[Skip]`/`[Superseded]`, no `deferred` issue. Plan: [`../plans/2026-06-07-font-size-control.md`](../plans/2026-06-07-font-size-control.md). Implementation PR open. In progress.

- [`2026-06-09-activity-rail-real-data-design.md`](2026-06-09-activity-rail-real-data-design.md) — Phase 7 (#137) real `received_events` actor feed replacing the mock rail. A dedicated `GET /api/activity` endpoint (always-200, degraded flag, session-gated, inbox-isolated) backed by `GitHubReceivedEventsReader` (fault-isolated) + `ActivityFeedBuilder` (verb map, bot tag, event-`id` dedup, 24h window, cap-50) + `ActivityProvider`. Frontend `useActivity` ~90s poll + last-good retention; `ActivityRail` Activity-only panel with transient bot toggle (default hidden), loading/empty/degraded/bot-filtered states, in-app `<Link>` w/ external fallback; Settings "Show activity rail" toggle (`inbox.showActivityRail`); single-panel P1 skeleton; e2e `FakeActivityProvider` (DI-swap, env+flag-gated). P2 (notifications + Watching) is a separate plan gated on a keep decision. Plan: [`../plans/2026-06-09-activity-rail-phase1.md`](../plans/2026-06-09-activity-rail-phase1.md). Implementation PR open. In progress.

- [`2026-06-10-285-inbox-unread-bar-design.md`](2026-06-10-285-inbox-unread-bar-design.md) — Phase 6 (#285) inbox "new changes" left-bar **reset-on-view**. Backend-only fix: the inbox snapshot froze the cheap local viewed-state together with the expensive GitHub feed, so `mark-viewed` (which writes a `TabStamp` to `state.json`) wasn't reflected until a full orchestrator refresh — the bar stayed lit after viewing. Fix extracts the viewed-state projection into one shared helper (`InboxViewedState.Project`, used by both refresh-time materialization and the read overlay) and applies `InboxViewedState.ApplyViewedState` as a read-only overlay on `GET /api/inbox` (loads live `AppState`, total-replacement re-projection; no GitHub refetch, no orchestrator mutation). **Symptom 2 reclassified as intended (owner reversal of the issue's original AC#2):** unviewed open-category PRs read unread on a fresh install — the current head-sha model already produces this, so the frontend is unchanged. Tests: `InboxViewedState` units + endpoint divergence guard (`Get_inbox_overlays_live_viewed_state_without_a_refresh`, RefreshCalls==0) + real-backend e2e (new `/test/seed-inbox` infra: fake `ISectionQueryRunner`/`IPrEnricher`/`ICiFailingDetector` under `PRISM_E2E_FAKE_REVIEW`, opt-in so existing baselines don't shift). `## Deferred work` records `[Skip]` A1/A2/GitHub-`last_read_at`. Plan: [`../plans/2026-06-10-285-inbox-unread-bar.md`](../plans/2026-06-10-285-inbox-unread-bar.md). Gated (UI-visual, owner B1). Implementation PR pending. In progress.

## Not started

_(none — all current specs are implemented or in progress.)_
