node_modules/
dist/
dist-test/
release/
dev-sidecar/
# Per-RID sidecar binary published here by CI / the local pack smoke before
# electron-builder runs. Self-contained (~80 MB+) — never commit it.
sidecar/
# Playwright _electron e2e output (traces, screenshots) — regenerated each run.
test-results/

# Framework-dependent dev sidecar published by scripts/run-desktop.* (NOT the
# packaging sidecar/ dir, which holds the self-contained CI-renamed artifact).
.dev-sidecar/
